diff --git a/src/components/flashcard/SelectionPopup.tsx b/src/components/flashcard/SelectionPopup.tsx
index 6ae3573..4e7a764 100644
--- a/src/components/flashcard/SelectionPopup.tsx
+++ b/src/components/flashcard/SelectionPopup.tsx
@@ -89,6 +89,7 @@ export type CanonicalSelectionResolution = {
   contentHash?: string;
   resolution:
     | "canonical-range"
+    | "quote-only-rich-pane"
     | "rich-pane-unique-quote"
     | "ambiguous-rich-pane"
     | "missing-canonical-surface";
@@ -133,21 +134,15 @@ export function resolveSelectionAgainstCanonicalSurface(
     }
   }
 
-  const full = canonicalRoot.textContent ?? "";
-  const uniqueHit = findUniqueQuoteOffsets(full, text);
-  if (uniqueHit) {
-    return {
-      blockText: full,
-      start: uniqueHit.start,
-      end: uniqueHit.end,
-      contentHash,
-      resolution: "rich-pane-unique-quote",
-    };
-  }
-
+  // Selection happened outside the canonical prose surface (for example inside
+  // a rendered table cell, clinical pearl, trap/keypoint callout, or margin
+  // note). Do NOT translate it to offsets in the hidden/compact canonical
+  // prose: that makes the saved annotation paint on the wrong surface. Store a
+  // quote-only anchor and let ReaderHighlightLayer re-resolve it against the
+  // visible rich surfaces first.
   return {
     contentHash,
-    resolution: "ambiguous-rich-pane",
+    resolution: "quote-only-rich-pane",
   };
 }
 
diff --git a/src/components/library-v2/FrameCardV2.tsx b/src/components/library-v2/FrameCardV2.tsx
index 70c53d1..0a9b39c 100644
--- a/src/components/library-v2/FrameCardV2.tsx
+++ b/src/components/library-v2/FrameCardV2.tsx
@@ -285,6 +285,7 @@ function FrameCardV2Impl({
     <div className="notion-block-body">
       {summaryNode && (
         <p
+          data-reader-annotation-surface="frame-summary"
           className={cn(
             "mb-2 text-[14.5px] leading-[1.65] text-lib-text-muted",
             "[&_strong]:font-[800] [&_strong]:text-lib-text/95",
@@ -371,6 +372,7 @@ function FrameCardV2Impl({
               </span>
               {titleNode && (
                 <h3
+                  data-reader-annotation-surface="frame-title"
                   className={cn(
                     "text-[16.5px] font-[700] leading-[1.5] tracking-[-0.006em] text-lib-text",
                     "[&_strong]:font-[780] [&_strong]:text-lib-text",
@@ -527,6 +529,7 @@ function PearlTail({
   return (
     <div
       data-frame-pearl
+      data-reader-annotation-surface="clinical-pearl"
       /* Explicit RTL — same belt-and-suspenders rationale as
          FrameCallouts: guarantees layout regardless of ancestor. */
       dir="rtl"
@@ -585,6 +588,7 @@ function MarginTail({
   return (
     <div
       data-frame-margin-note
+      data-reader-annotation-surface="margin-note"
       /* Explicit RTL to match PearlTail / FrameCallouts. */
       dir="rtl"
       className={cn(
@@ -736,6 +740,7 @@ function PlainListItems({
   if (!frame.listItems?.length) return null;
   return (
     <ul
+      data-reader-annotation-surface="list-items"
       className={cn(
         /* CSS variable for fullscreen-aware width control.
            Default: 70ch. App can set `--reader-prose-w` on an
@@ -901,6 +906,7 @@ function FrameCallouts({
           <div
             key={`${c.kind}-${c.order}-${i}`}
             data-callout-kind={c.kind}
+            data-reader-annotation-surface={`callout-${c.kind}`}
             /* Explicit dir="rtl" on each callout: the section-level
                dir="rtl" alone wasn't propagating through deeper
                ancestor chains in some projects. Setting it here
@@ -962,7 +968,10 @@ function FrameTable({
   rows: { text: string; bold?: boolean }[][];
 }) {
   return (
-    <div className="my-3 -mx-1 overflow-x-auto">
+    <div
+      data-reader-annotation-surface="table"
+      className="my-3 -mx-1 overflow-x-auto"
+    >
       <table className="w-full border-collapse text-[14.25px]">
         <thead>
           <tr>
diff --git a/src/components/library-v2/ReaderHighlightLayer.tsx b/src/components/library-v2/ReaderHighlightLayer.tsx
index 062f79b..8f7588c 100644
--- a/src/components/library-v2/ReaderHighlightLayer.tsx
+++ b/src/components/library-v2/ReaderHighlightLayer.tsx
@@ -2,10 +2,7 @@
 
 import { useEffect, useRef, type RefObject } from "react";
 
-import {
-  resolveAnchorRange,
-  resolveAnchorRangeByFrameId,
-} from "@/lib/local-first/anchorResolver";
+import { resolveAnchorRangeByFrameId } from "@/lib/local-first/anchorResolver";
 import type { ReaderAnnotation } from "@/hooks/useReaderAnnotations";
 
 /**
@@ -63,13 +60,82 @@ function bucketName(ann: ReaderAnnotation): string {
   return ""; // comments handled by NoteMarkerLayer in 3b
 }
 
+function resolveRangeWithinSurface(
+  surface: HTMLElement,
+  start: number,
+  end: number,
+): Range | null {
+  if (!Number.isFinite(start) || !Number.isFinite(end)) return null;
+  if (start < 0 || end < start) return null;
+
+  const surfaceLen = (surface.textContent ?? "").length;
+  if (end > surfaceLen) return null;
+
+  const walker = document.createTreeWalker(surface, NodeFilter.SHOW_TEXT);
+  let acc = 0;
+  let startNode: Node | null = null;
+  let startOff = 0;
+  let endNode: Node | null = null;
+  let endOff = 0;
+  let lastNode: Node | null = null;
+  let lastNodeLen = 0;
+
+  let node: Node | null;
+  while ((node = walker.nextNode())) {
+    const len = node.textContent?.length ?? 0;
+    const nodeEnd = acc + len;
+
+    if (startNode === null && start <= nodeEnd) {
+      startNode = node;
+      startOff = Math.max(0, start - acc);
+    }
+    if (end <= nodeEnd) {
+      endNode = node;
+      endOff = Math.max(0, end - acc);
+      break;
+    }
+
+    acc = nodeEnd;
+    lastNode = node;
+    lastNodeLen = len;
+  }
+
+  if (!endNode && lastNode && end === acc) {
+    endNode = lastNode;
+    endOff = lastNodeLen;
+  }
+  if (!startNode && lastNode && start === acc + lastNodeLen) {
+    startNode = lastNode;
+    startOff = lastNodeLen;
+  }
+  if (!startNode || !endNode) return null;
+
+  try {
+    const range = document.createRange();
+    range.setStart(startNode, startOff);
+    range.setEnd(endNode, endOff);
+    return range;
+  } catch {
+    return null;
+  }
+}
+
+function findRangeInSurfaceByQuote(surface: HTMLElement, quote: string): Range | null {
+  const text = surface.textContent ?? "";
+  const idx = text.indexOf(quote);
+  if (idx < 0) return null;
+  return resolveRangeWithinSurface(surface, idx, idx + quote.length);
+}
+
 function findRangeByQuote(
   scope: ParentNode,
   frameId: string | null,
   quote: string,
 ): Range | null {
-  // Legacy fallback: stored row has no offsets. Find the quote inside the
-  // frame's canonical surface (or the frame itself), pick the first match.
+  // Quote-only fallback: stored row has no offsets. Search visible rich
+  // surfaces first (tables, callouts, pearl/trap boxes), then canonical prose.
+  // This keeps annotations created inside rendered structures painted where
+  // the user selected them instead of falling back to the canonical text copy.
   if (!quote.trim()) return null;
   const escaped =
     typeof CSS !== "undefined" && "escape" in CSS
@@ -79,13 +145,27 @@ function findRangeByQuote(
     ? scope.querySelector<HTMLElement>(`[data-frame-id="${escaped}"]`)
     : null;
   if (!frame) return null;
-  const surface =
+
+  const richSurfaces = Array.from(
+    frame.querySelectorAll<HTMLElement>("[data-reader-annotation-surface]"),
+  );
+  for (const surface of richSurfaces) {
+    const range = findRangeInSurfaceByQuote(surface, quote);
+    if (range) return range;
+  }
+
+  const canonicalSurface =
     frame.querySelector<HTMLElement>("[data-anchor-surface='canonical']") ??
-    frame;
-  const text = surface.textContent ?? "";
-  const idx = text.indexOf(quote);
-  if (idx < 0) return null;
-  return resolveAnchorRange(frame, idx, idx + quote.length);
+    null;
+  if (canonicalSurface) {
+    const range = findRangeInSurfaceByQuote(canonicalSurface, quote);
+    if (range) return range;
+  }
+
+  const frameRange = findRangeInSurfaceByQuote(frame, quote);
+  if (frameRange) return frameRange;
+
+  return null;
 }
 
 interface CSSHighlightRegistry {
diff --git a/src/components/library-v2/SegmentRenderer.tsx b/src/components/library-v2/SegmentRenderer.tsx
index 1f3c09f..e719a9b 100644
--- a/src/components/library-v2/SegmentRenderer.tsx
+++ b/src/components/library-v2/SegmentRenderer.tsx
@@ -166,6 +166,7 @@ export function SegmentRenderer({
               <section
                 dir="rtl"
                 data-section-closing-keypoint
+                data-reader-annotation-surface="section-closing-keypoint"
                 className={[
                   "mt-4 rounded-[8px] border-s-[3px]",
                   "border-y border-e border-black/[0.04] dark:border-white/[0.04]",
